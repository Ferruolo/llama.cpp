#include "ggml.h"
#include "train.h"


#include <cstdlib>
#include <cstring>


int main(int argc, char** argv) {
    if (argc < 2) {
        fprintf(stderr, "usage: ./llamacheck \"string\" \n");
        return 1;
    }

    // Tokenize String, add BEGIN token, END token

    // Proccess string

    // Add BEGIN token, see new output

    // print new output and compare

    return 0;
}